export default function PackagesPage() {
  return (
    <section className="rounded-lg border border-dashed border-mist/15 px-6 py-16 text-center">
      <p className="text-sm font-semibold text-mist">Packages</p>
      <p className="mt-1 text-xs text-muted-dark">
        Package tracking lands in the next update (SP02).
      </p>
    </section>
  );
}
